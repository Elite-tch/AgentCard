export async function notifyOwnerApprovalNeeded(opts: any) {
  console.log(`[stub] notifyOwnerApprovalNeeded:`, opts);
}

export async function checkSpendAlert(apiKeyId: string, amount: number) {
  console.log(`[stub] checkSpendAlert:`, { apiKeyId, amount });
}
